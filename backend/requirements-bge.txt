# Optional local embedding stack. Keep separate because PyTorch/model files are large.
#
# 安装顺序很重要：先满足 numpy 约束，再装 sentence-transformers，
# 否则 pip 可能拉进 numpy 2.x，导致 scipy/sklearn ABI 不兼容、
# BGE 静默降级为哈希向量（2026-09-12 事故，排查见 scripts/_check_bge_health.py）。
#   pip install -r requirements-bge.txt
# 装完务必验证：python scripts/_check_bge_health.py
numpy>=1.24,<2
modelscope>=1.20.0
sentence-transformers>=3.0.0
