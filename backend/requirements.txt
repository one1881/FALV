fastapi==0.115.5
uvicorn==0.32.1
sqlalchemy==2.0.36
pg8000==1.31.2
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator>=2.1.0  # pydantic EmailStr 依赖，缺失会导致所有路由模块 import 失败
jinja2>=3.1.0  # 合同/报告 HTML 渲染依赖，缺失会导致 auth/review/litigation 等路由 import 失败
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
openai>=1.0.0
httpx>=0.27.0
# AI 多智能体链路（deepagents_service 顶层依赖，原缺失导致后端无法启动）
deepagents
langchain-openai>=0.2.0

# 可选：语义向量检索（BGE）。未安装时 vector_search_server 自动降级为 hash 相似度，
# 功能不中断但相似合同/案例检索召回质量下降（体积较大，按需开启）。
# sentence-transformers>=2.2.0
