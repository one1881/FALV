fastapi==0.115.5
uvicorn==0.32.1
sqlalchemy==2.0.36
pg8000==1.31.2
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator>=2.1.0  # pydantic EmailStr 依赖，缺失会导致所有路由模块 import 失败
jinja2>=3.1.0  # 合同/报告 HTML 渲染依赖，缺失会导致 auth/review/litigation 等路由 import 失败
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
openai>=1.0.0
httpx>=0.27.0
# numpy 必须锁 <2（2026-09-12 事故）：scipy 1.13.1 / contourpy 1.2.0 / numba 0.60.0 /
# gensim 4.3.3 / paddlex 3.7.2 都是按 numpy 1.x ABI 编译的。装 numpy 2.x 会让
# sklearn -> sentence-transformers 直接 ImportError，BGE 静默降级成哈希向量
# （不报错、只打 warning，相似检索质量长期下降）。排查见 scripts/_check_bge_health.py。
numpy>=1.24,<2
# AI 多智能体链路（deepagents_service 顶层依赖，原缺失导致后端无法启动）
deepagents
langchain-openai>=0.2.0

# 可选：语义向量检索（BGE）。未安装时 vector_search_server 自动降级为 hash 相似度，
# 功能不中断但相似合同/案例检索召回质量下降（体积较大，按需开启）。
# 依赖清单见 requirements-bge.txt（含 sentence-transformers 与 numpy 约束）。
